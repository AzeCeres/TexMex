namespace OmniDi.Library.Util
{
    public static class Delegates
    {
        /// <summary>
        /// A delegate for use for sending simple notifications between classes.
        /// </summary>
        public delegate void Notification();
    }
}